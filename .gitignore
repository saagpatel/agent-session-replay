# dependencies
node_modules/

# build output
dist/
.vite/

# rust / tauri — generated; the scaffold + icons under src-tauri/ ARE committed
src-tauri/target/
src-tauri/gen/
# desktop-only app — skip the mobile icon variants `tauri icon` also emits
src-tauri/icons/android/
src-tauri/icons/ios/

# never commit real transcript data (privacy-first)
real-samples/
*.real.jsonl
# local-only visual-QA harness — pre-parses real transcripts into JSON fixtures
# that embed real local paths; never committed.
qa/

# os / editor
.DS_Store
*.local
coverage/
