{
	"//": "Typecheck gate for shippable app code (src/ui + src/core + src/watchdog). esbuild/vite strip types without checking them; this is the real type gate. Tests are excluded and verified by actually running them (node --test).",
	"extends": "./tsconfig.json",
	"include": ["src"],
	"exclude": ["**/*.test.ts"]
}
